# MockBank API

FastAPI + PostgreSQL mock banking API.

Docs available at `/docs`.

## Setup (local dev)

1. Create Python venv, install:
   ```bash
   python -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt
